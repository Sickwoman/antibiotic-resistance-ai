# Dependencies up to Version 0.2 (data acquisition, exploration, preprocessing, splits).
# Later versions add lightgbm/xgboost, torch, shap, fastapi, uvicorn only when needed.
# Exact versions of the tested environment are recorded in requirements-lock.txt.
numpy>=1.26
pandas>=2.2
matplotlib>=3.8
pyyaml>=6.0
scikit-learn>=1.5    # V0.2: StratifiedGroupKFold for patient-grouped splits (brings scipy)
pytest>=8.0
ruff==0.16.8         # linter (python -m ruff check .); pinned so new rules never break CI unexpectedly
notebook>=7.0
nbformat>=5.9        # notebook validation in tests
ipykernel>=6.29
